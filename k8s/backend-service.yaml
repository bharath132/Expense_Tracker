apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: expense-tracker
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: expense-tracker
    tier: backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
