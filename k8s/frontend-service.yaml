apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: expense-tracker
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    app: expense-tracker
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
